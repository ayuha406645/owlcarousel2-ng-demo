{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "baseUrl": "./",
    "types": [
      "jquery",
      "owl.carousel"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
